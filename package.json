{
  "name": "aplat",
  "version": "1.0.0",
  "private": true,
  "description": "APlat · Servicios digitales de última generación",
  "scripts": {
    "dev": "pnpm --filter aplat-web dev",
    "dev:api": "pnpm --filter aplat-api dev",
    "build": "pnpm --filter aplat-web build",
    "build:api": "pnpm --filter aplat-api build",
    "start": "pnpm --filter aplat-web start",
    "start:api": "pnpm --filter aplat-api start",
    "lint": "pnpm --filter aplat-web lint"
  },
  "packageManager": "pnpm@10.28.2"
}
