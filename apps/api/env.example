# APlat API · Variables de entorno
# Copia a .env y ajusta valores.

PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Directorio de datos: aquí se guardan SQLite (clientes, auditoría, presence) y JSON (Passkey, reportes, etc.).
# En producción monta un volumen persistente y usa ej. /data para que todo quede guardado permanentemente.
# APLAT_DATA_PATH=/data

# Cron: secreto para ejecutar cortes automáticos (POST /api/cron/process-cutoffs). Ejecutar cada día a las 23:59.
# APLAT_CRON_SECRET=genera-un-secreto-aleatorio

# Orígenes permitidos para CORS (separados por coma o * para todos).
# En producción usa la URL de tu frontend (ej. https://aplat.vercel.app).
CORS_ORIGIN=http://localhost:3000,https://tu-dominio.vercel.app

# Auth (login). En producción usa valores seguros.
APLAT_JWT_SECRET=genera-con-openssl-rand-hex-32
APLAT_ADMIN_EMAIL=admin@aplat.local
APLAT_ADMIN_PASSWORD=APlat2025!

# WebAuthn / Passkey (opcional). RP ID debe coincidir con el dominio del front (ej. aplat.vercel.app).
# Por defecto el store de Passkey es APLAT_DATA_PATH/webauthn-store.json (mismo volumen persistente).
# APLAT_WEBAUTHN_RP_ID=aplat.vercel.app
# APLAT_WEBAUTHN_STORE_PATH=/data/webauthn-store.json
