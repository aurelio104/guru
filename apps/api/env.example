# GURU API · Variables de entorno
# Copia a .env y ajusta valores.

PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Directorio de datos: aquí se guardan SQLite (clientes, auditoría, presence) y JSON (Passkey, reportes, etc.).
# En producción monta un volumen persistente y usa ej. /data para que todo quede guardado permanentemente.
# GURU_DATA_PATH=/data

# Cron: secreto para ejecutar cortes automáticos (POST /api/cron/process-cutoffs). Ejecutar cada día a las 23:59.
# GURU_CRON_SECRET=genera-un-secreto-aleatorio

# Orígenes permitidos para CORS (separados por coma o * para todos).
# En producción usa la URL de tu frontend (ej. https://guru.vercel.app).
CORS_ORIGIN=http://localhost:3000,https://tu-dominio.vercel.app

# Auth (login). En producción usa valores seguros.
GURU_JWT_SECRET=genera-con-openssl-rand-hex-32
GURU_ADMIN_EMAIL=admin@guru.local
GURU_ADMIN_PASSWORD=GURU2025!

# WebAuthn / Passkey (opcional). RP ID debe coincidir con el dominio del front (ej. guru.vercel.app).
# Por defecto el store de Passkey es GURU_DATA_PATH/webauthn-store.json (mismo volumen persistente).
# GURU_WEBAUTHN_RP_ID=guru.vercel.app
# GURU_WEBAUTHN_STORE_PATH=/data/webauthn-store.json
